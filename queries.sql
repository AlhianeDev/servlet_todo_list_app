/*

CREATE TABLE Todos (
	
    todo_id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    
    todo_title VARCHAR(255) NOT NULL unique,
    
	created_date VARCHAR(255) NOT NULL,
    
    is_of_new_day BOOLEAN NOT NULL,
    
    is_confirmed BOOLEAN NOT NULL

);

*/

# INSERT INTO Todos (todo_title, is_of_new_day) VALUES ("HOHOHOHO", true);

# SELECT * FROM Todos ORDER BY todo_id DESC LIMIT 1;

# TRUNCATE TABLE Todos;
